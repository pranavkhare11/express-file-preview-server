{
  "name": "express-file-preview-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter \"*\" dev",
    "dev:server": "pnpm --filter @preview/server dev",
    "dev:client": "pnpm --filter @preview/client dev",
    "test": "pnpm --filter @preview/server test"
  }
}
